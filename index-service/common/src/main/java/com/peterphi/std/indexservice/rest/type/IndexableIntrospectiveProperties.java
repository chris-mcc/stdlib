package com.peterphi.std.indexservice.rest.type;

import com.peterphi.std.annotation.Doc;

public class IndexableIntrospectiveProperties {
    @Doc("The database url used by the application")
    public static final String DATABASE_URL = "database.url";
    @Doc("The database product name used by the application")
    public static final String DATABASE_PRODUCT = "database.product";
    @Doc("The database product name used by the application")
    public static final String DATABASE_USERNAME = "database.user.name";
}
